name: desa

services:
  postgres:
    image: postgres:16.0-alpine
    platform: linux/amd64
    container_name: desa-postgres
    environment:
      POSTGRES_DB: desa
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - desa_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  desa_postgres_data:
